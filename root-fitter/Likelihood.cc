#include "Likelihood.hh"
ClassImp(Likelihood);

/*Gaussian Likelihood::MakeGaussianNusiance(std::string name, double mean, double sigma) const{
  Gaussian g(name, mean, sigma);
  return g;
}*/



double Likelihood::CalculateLikelihood(double u)
{
  return 0.0;
}
